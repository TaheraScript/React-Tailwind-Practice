/* 1.Explain in 2–3 sentences why props should never be modified inside a child component. */
/* Props should never be modified inside a child component because they represent data owned and controlled by the parent component — the child is only meant to read and display that data, not change it. If a child could freely modify its props, React would lose track of where the actual source of truth is, making the app's data flow unpredictable and hard to debug. This is why React treats props as immutable, and if a component needs to manage data that changes over time, it should use state instead, which is designed specifically for values that can update. */
