/* 1.Define, in your own words, what a component is?*/
/* component is a javascript function.a building block of component builds a react.we can reuse a component by different props,instead of rewriting the code.each component handles an specific piece of UI(like a button,card or form) making the app easier to manage as it grows.when a components data changes only that component re-renders not the whole page which makes it faster */

/* Create a new functional component called WelcomeCard.tsx that returns a div containing a heading and a paragraph. */
